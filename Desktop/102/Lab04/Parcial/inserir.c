#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main (int argc, char *argv[]) {
	
	tipo--;
	if (controle[tipo] == 10)
		return 0;
		
	scanf(" %[^\n]s", nome[controle[tipo]][0]);
	scanf("%lld", &cpf[controle[tipo]][0]);
	scanf(" %[^\n]s", end[controle[tipo]][0]);
	scanf("%lld %lld", &foneres[controle[tipo]][0], &fonecel[controle[tipo]][0]);
		controle[tipo]++;
	
	return 1; 
}
